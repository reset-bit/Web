const cartReducer = (state = { list: [], isInit: false }, action = {}) => {
        switch(action.type) {
                default:
                        return state;
        }
};

export default cartReducer;